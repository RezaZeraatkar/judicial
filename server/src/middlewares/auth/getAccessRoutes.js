const jwt = require("jsonwebtoken");
const handleError = require("../../utils/handleError");

require("dotenv").config();

function checkAccessRoutes(req, res, next) {}
module.exports = checkAccessRoutes;
